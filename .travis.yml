language: java
sudo: false
#install: true

jdk:
  - oraclejdk8

#script:
  # JaCoCo is used to have code coverage, the agent has to be activated
  #- mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar
#  - sonar-scanner

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'


#addons:
#  sonarqube:true
#   token:
#      secure: "4b20bb10221113c1a88e1c119277a306589dd58d"

addons:
  sonarqube: true
install:
- mvn dependency:go-offline
script:
- mvn verify site sonar:sonar -Dmaven.test.failure.ignore=true


notifications:
  email: false
